# Reflection Questions

A. The structure for the fastest for insertions at the end was the list

B. The structure for the fastest inserts at the start was also the list

C. The best option would be a list, as you would need to resize an array each time youd need to expand the array. I.E. reallocate the entire array when you resize.

![Console Output](ConsoleOutput.png)